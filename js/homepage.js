import { createPopper } from '@popperjs/core';
const popcorn = document.querySelector('#AC1');
const tooltip = document.querySelector('#tooltipAC');
createPopper(AC1, tooltipAC, {
  placement: 'top',
});